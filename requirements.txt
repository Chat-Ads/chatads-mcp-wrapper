# ChatAds MCP Wrapper Dependencies
# Python 3.10+ required

# MCP Framework
fastmcp==0.4.1

# HTTP Client with timeout and retry support
httpx==0.27.2

# Data validation & settings management
pydantic==2.12.0

# JSON logging for production environments (optional but recommended)
python-json-logger==2.0.7

# Development/Testing dependencies (install separately with -r requirements-dev.txt)
# pytest==8.3.3
# pytest-cov==6.0.0
# pytest-httpx==0.35.0
# pytest-asyncio==0.24.0
